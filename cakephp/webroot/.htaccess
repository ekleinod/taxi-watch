<IfModule mod_rewrite.c>
		RewriteEngine On
		RewriteBase /var/www/html/taxi-watch/
		RewriteCond %{REQUEST_FILENAME} !-f
		RewriteCond %{REQUEST_URI} !^/(webroot/)?(img|css|js)/(.*)$
		RewriteRule ^ index.php [L]
</IfModule>
